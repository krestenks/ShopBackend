# application.properties
# allow overriding via environment / CLI
spring.datasource.url=jdbc:sqlite:${DB_PATH:./ShopManager.db}
spring.datasource.driver-class-name=org.sqlite.JDBC


# example server port usage: your app might be composed of 3 separate services; if it's a single process
# that opens all ports, ensure it reads these properties. Adjust names to your code.
server.port=${SERVER_PORT:9090}


# If you run three separate runnables, use different profiles/args when starting.